import { ContentAgePipe } from './content-age.pipe';

describe('ContentAgePipe', () => {
  it('create an instance', () => {
    const pipe = new ContentAgePipe();
    expect(pipe).toBeTruthy();
  });
});
